# TraceLine Frontend

Angular 21 frontend for TraceLine.

```bash
npm install
npm start
npm run test:ci
npm run e2e -- --project=chromium
npm run build
```

The dev server proxies `/api` to `http://127.0.0.1:8080`.
